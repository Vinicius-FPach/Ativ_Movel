type Carrer = {
  id?: string; // reserved for firestore id
  name: string;
  trophies: number;
  topscorer: string;
  vicetopscorer: string;
  topassist: string;
  viceassist: string;
};

export default Carrer;
